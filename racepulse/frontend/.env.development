# 개발 환경 환경변수 (로컬 서버 주소)
# 이 파일은 git에 커밋해도 됩니다 — 민감 정보 없음

# Spring Boot BE 서버 (API 게이트웨이)
VITE_API_BASE_URL=http://localhost:8080/api/v1

# FastAPI ML 서버 (날씨 등 직접 호출용)
VITE_ML_SERVER_URL=http://localhost:8000
