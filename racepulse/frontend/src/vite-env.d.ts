/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// =============================================================================
// vite-env.d.ts — Vite와 플러그인이 제공하는 가상 모듈 타입 선언 모음
// =============================================================================
// 왜 필요한가?
// `virtual:pwa-register` 같은 가상 모듈은 실제 파일이 아니라 플러그인이 만들어 주기 때문에
// TypeScript에게 "이 모듈은 정상이다"라고 알려주는 참조 선언이 꼭 필요합니다.
// =============================================================================
