# 운영 환경 환경변수
# 실제 도메인으로 배포 전 수정하세요.
# 이 파일은 git에 커밋해도 됩니다 — 민감 정보 없음 (실제 키는 CI/CD 시크릿 사용)

# Nginx가 /api/v1 을 Spring Boot로 프록시하므로 상대 경로를 사용합니다.
VITE_API_BASE_URL=/api/v1

# FastAPI ML 서버 (실제 도메인으로 교체)
VITE_ML_SERVER_URL=https://ml.racepulse.com
