[pytest]
# asyncio_mode = auto 로 설정하면 async def 테스트에
# @pytest.mark.asyncio 를 붙이지 않아도 자동으로 실행됩니다.
# 여기서는 명시적으로 붙이는 방식을 사용하므로 strict 모드로 설정합니다.
asyncio_mode = auto

# 테스트 파일 경로
testpaths = tests

# 출력 설정: -v 없이도 테스트 이름을 표시합니다.
addopts = -v
