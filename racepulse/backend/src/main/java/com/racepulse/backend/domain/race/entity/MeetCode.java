package com.racepulse.backend.domain.race.entity;

// =============================================================================
// MeetCode.java — 경마장 코드를 자바 enum으로 안전하게 관리하는 타입
// =============================================================================
// 문자열("SC", "BU", "JJ")을 코드 곳곳에 흩뿌리면 오타가 나기 쉽습니다.
// enum으로 묶어두면 허용된 값만 사용할 수 있어 안정성이 올라갑니다.
// =============================================================================

public enum MeetCode {
    SC,
    BU,
    JJ
}
